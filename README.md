### FINAL PROJECT HYF BACK END JAVA 2026

You have to set the DB environement by creating a `env.properties` at the root of the project (copy paste `EXAMPLE-env.properties` and remove `EXAMPLE-`) then replace the values by yours
You might want to modify the pom.xml using the dependencty corresponding to the DB you're using (in my case it's mysql) 
